<h1>Add a new article</h1>

<% if @article.errors.any? %>
    <h2>The following error(s) prevented the article from being saved:</h2>
    <% @article.errors.full_messages.each do |message| %>
        <ul>
            <li><%= message %></li>
        </ul>
    <% end %>
<% end %>

<%= form_with scope: :article, url: articles_path, data: { turbo: false }, local: true do |f| %>

    <p>
        <%= f.label :title %><br>
        <%= f.text_field :title %>
    </p>

    <p>
        <%= f.label :description %><br>
        <%= f.text_area :description %>
    </p>

    <p>
        <%= f.submit "Save article" %>
    </p>
<% end %>

<p><%= link_to 'Cancel and return to list of articles', articles_path %></p>